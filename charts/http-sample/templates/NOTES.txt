The {{ include "http-sample.componentName" . }} component has been deployed successfully.

The HTTP Server is serving on {{ .Values.component.hostname }}.

If the component is serving on the default port 9091, port-forward and connect locally with:

   kubectl -n cosmonic-system port-forward svc/hostgroup-{{ .Values.httpProvider.hostgroup }} 9091:9091

   curl localhost:9091
