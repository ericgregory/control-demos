apiVersion: runtime.wasmcloud.dev/v1alpha1
kind: Config
metadata:
  name: {{ .Values.configProvider.name }}
spec:
  config:
    - name: {{ .Values.configProvider.configName }}
      value: {{ .Values.configProvider.configValue }}